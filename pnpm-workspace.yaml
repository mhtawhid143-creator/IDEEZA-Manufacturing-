packages:
  - "apps/*"
  - "packages/*"

# esbuild ships the platform binary as a dependency; its install script is
# allowed so that vitest can run without a manual approval step.
allowBuilds:
  esbuild: true
