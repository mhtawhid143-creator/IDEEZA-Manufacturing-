# The workspace resolves peers deliberately and pins what matters in the
# lockfile. Both of these match how pnpm-lock.yaml was produced.
#
# Which dependencies may run install scripts is not here: pnpm 11 keeps that in
# pnpm-workspace.yaml under allowBuilds.
strict-peer-dependencies=false
auto-install-peers=true
